// addTwoNumbers Exercise
function addTwoNumbers(a, b) {
  return a + b;
}

const addTwoNumbersResult = addTwoNumbers(2, 2);
console.log('addTwoNumbers Exercise:', addTwoNumbersResult);

// convertHoursToMinutes Exercise
function convertHoursToMinuts(hours) {
  return hours * 60;
}

const convertHoursToMinutsResults = convertHoursToMinuts(2);
console.log('convertHoursToNumbers Exercise:', convertHoursToMinutsResults);

// getGreeting Exercise
function getGreeting(name) {
  const greeting = 'Hello ' + name + '!';
  return greeting;
}

const getGreetingResults = getGreeting('World');
console.log('getGreeting Exercise:', getGreetingResults);

// addAndMultiplyBy5 Exercise
function addAndMultiplyBy5(num1, num2) {
  return (num1 + num2) * 5;
}

const addAndMultiplyBy5Results = addAndMultiplyBy5(10, 5);
console.log('addAndMultiplyBy5 Excercise:', addAndMultiplyBy5Results);

// multiplyAndDivideBy5 Exercise
function multiplyAndDivideBy5(num1, num2) {
  return (num1 * num2) / 5;
}

const multiplyAndDivideBy5Results = multiplyAndDivideBy5(35, 10);
console.log('multiplyAndDivideBy5 Exercise:', multiplyAndDivideBy5Results);

// subtractTwoNumbers Exercise
function subtractTwoNumbers(num1, num2) {
  return num1 - num2;
}

const subtractTwoNumbersResults = subtractTwoNumbers(22, 7);
console.log('subtractTwoNumbers Exercise:', subtractTwoNumbersResults);

// getCircleCircumference Exercise
function getCircleCircumference(radius) {
  const pie = 3.141592653589793;
  return 2 * pie * radius;
}

const getCircleCircumferenceResults = getCircleCircumference(5);
console.log('getCircleCircumference Exercise:', getCircleCircumferenceResults);

// getFullName Exercise
function getFullName(firstName, lastName) {
  return firstName + ' ' + lastName;
}

const getFullNameResults = getFullName('Juan', 'Ramirez');
console.log('getFullName Exercise:', getFullNameResults);

// cube Exercise
function cube(number) {
  return number * number * number;
}

const cubeResults = cube(5);
console.log('cube Exercise:', cubeResults);
